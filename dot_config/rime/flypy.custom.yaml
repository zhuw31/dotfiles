patch:
  key_binder/bindings:
    # - { accept: "Shift+space", toggle: noop, when: always } #取消全半角切换
    - { accept: "Control+j", toggle: noop, when: always } #取消简繁切换
    - { accept: semicolon, send: 2, when: has_menu }
